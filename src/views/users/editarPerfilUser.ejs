<!DOCTYPE html>
<html lang="en">
<head>
    <title>Perfil de usuario</title>
    <link rel="stylesheet" href="/css/perfil.css"></link>
    <%-include("../partials/head")%>
    <%-include("../partials/header")%>
</head>
<body class="body">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col col-lg-10 mb-8 mb-lg-4">
        <div class="card mb-3" style="border-radius: 8px;">
          <div class="row g-0">
            <div class="col-md-4 gradient-custom text-center text-white"
              style="border-top-left-radius: .5rem; border-bottom-left-radius: .5rem;">
              <form action="/editarPerfilUser/<%= usuario.id%>?_method=PUT" method="POST">
                <img src="<%=usuario.imagen%>" alt="Imagen de usuario" class="rounded-circle imagenUser" width="100 px">
                <h5><%=usuario.nombre%> <%=usuario.apellido%></h5>
                <p><%=usuario.Rol.nombre%></p>
            </div>
            <div class="col-md-8">
              <div class="card-body p-4">
                <h6>Información personal</h6>
                <hr class="mt-0 mb-4">
                <div class="row pt-1">
                  <div class="col-6 mb-3">
                    <h6>Nombre</h6>
                    <p class="text-muted"><%=usuario.nombre%></p>
                  </div>
                  <div class="col-6 mb-3">
                    <h6>Apellidos</h6>
                    <p class="text-muted"><%=usuario.apellido%></p>
                  </div>
                </div>
                <div class="row pt-1">
                  <div class="col-6 mb-3">
                    <h6>Email</h6>
                    <p class="text-muted"><%=usuario.email%></p>
                  </div>
                  <div class="col-6 mb-3">
                    <h6>Contraseña</h6>
                    <input required type="password" id="contrasena" name="contrasena" class="form-control required" placeholder="Ingrese una contraseña" pattern="(?=^.{8,}$)(?=.*\d)(?=.*\W+)(?![.\n])(?=.*[A-Z])(?=.*[a-z]).*$" title="La contraseña debe ser de al menos 8 caracteres y tener letras mayúsculas, minúsculas, un número y un carácter especial."/>
                  </div>
                </div>
                <div class="d-flex justify-content-center">
                  <button type="submit" class="boton">Actualizar</button>
                </div>
                <div class="d-flex justify-content-center">
                  <a href="https://www.facebook.com/"><i class="fab fa-facebook-f fa-lg me-3 iconos"></i></a>
                  <a href="https://twitter.com/"><i class="fab fa-twitter fa-lg me-3 iconos"></i></a>
                  <a href="https://www.instagram.com/"><i class="fab fa-instagram fa-lg iconos"></i></a>
                </div>
              </form> 
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>