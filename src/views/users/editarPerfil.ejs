<!DOCTYPE html>
<html lang="en">
<head>
    <title>Perfil de usuario</title>
    <link rel="stylesheet" href="/css/perfil.css"></link>
    <%-include("../partials/head")%>
    <%-include("../partials/header")%>
</head>
<body class="body">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col col-lg-10 mb-8 mb-lg-4">
        <div class="card mb-3" style="border-radius: 8px;">
          <div class="row g-0">
            <div class="col-md-4 gradient-custom text-center text-white"
              style="border-top-left-radius: .5rem; border-bottom-left-radius: .5rem;">
              <form action="/editarPerfil/<%= usuario.id%>?_method=PUT" method="POST" enctype="multipart/form-data">
                <img src="<%=usuario.imagen%>" alt="Imagen de usuario" class="rounded-circle imagenUser" width="100 px">
                <h5><%=usuario.nombre%> <%=usuario.apellido%></h5>
                <p>ROL</p>
            </div>
            <div class="col-md-8">
              <div class="card-body p-4">
                <h6>Información personal</h6>
                <hr class="mt-0 mb-4">
                <div class="row pt-1">
                  <div class="col-6 mb-3">
                    <h6>Nombre</h6>
                    <!--same as in registro-->
                    <input type="text" id="nombre" name="nombre" class="form-control required" minlength="2" maxlength="20" pattern="[A-Za-z ]{2,20}" title="El nombre debe tener entre 2 y 20 caracteres y solo puede contener letras" required>
                  </div>
                  <div class="col-6 mb-3">
                    <h6>Apellidos</h6>
                    <input type="text" id="apellido" name="apellido" class="form-control required" minlength="2" maxlength="20" pattern="[A-Za-z ]{2,20}" title="El apellido debe tener entre 2 y 20 caracteres y solo puede contener letras" required>
                  </div>
                </div>
                <div class="row pt-1">
                  <div class="col-6 mb-3">
                    <h6>Email</h6>
                    <p class="text-muted"><%=usuario.email%></p>
                  </div>
                  <div class="col-6 mb-3">
                    <h6>Contraseña</h6>
                    <input type="password" name="password" class="form-control password1 required" value="<%=usuario.email%>" placeholder="" />
                    <span class="fa fa-fw fa-eye password-icon show-password"></span>
                  </div>
                  <div class="col-8 mb-4">
                    <h6>Imagen</h6>
                    <input type="file" name="imagen" id="imagen" multiple accept="image/jpeg, image/jpg, image/png, image/gif" /></input>
                  </div>
                </div>
                <div class="d-flex justify-content-center">
                  <button type="submit" class="boton">Actualizar</button>
                </div>
                <div class="d-flex justify-content-center">
                  <a href="https://www.facebook.com/"><i class="fab fa-facebook-f fa-lg me-3 iconos"></i></a>
                  <a href="https://twitter.com/"><i class="fab fa-twitter fa-lg me-3 iconos"></i></a>
                  <a href="https://www.instagram.com/"><i class="fab fa-instagram fa-lg iconos"></i></a>
                </div>
              </form> 
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>