<!DOCTYPE html>
<html lang="en">
<head>
    <title>Perfil de usuario</title>
    <link rel="stylesheet" href="/css/perfil.css"></link>
    <%-include("../partials/head")%>
</head>
<body class="body">
  <%-include("../partials/header")%>
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col col-lg-10 mb-8 mb-lg-4">
        <div class="card mb-3" style="border-radius: 8px;">
          <div class="row g-0">
            <div class="col-md-4 gradient-custom text-center text-white"
              style="border-top-left-radius: .5rem; border-bottom-left-radius: .5rem;">
              <img src="<%=usuario.imagen%>" alt="Imagen de usuario" class="rounded-circle imagenUser" width="100 px">
              <h5><%=usuario.nombre%> <%=usuario.apellido%></h5>
              <p>ROL</p>
              <a href="/editarPerfil/<%=usuario.id%>"><i class="far fa-edit mb-5"></i> Editar Información de Usuario</a>
              <form action="/<%= usuario.id%>?_method=DELETE" method="POST">
                <button type="submit" id="eliminarProducto" class="boton">Eliminar Usuario</button>
              </form>
            </div>
            <div class="col-md-8">
              <div class="card-body p-4">
                <h6>Información personal</h6>
                <hr class="mt-0 mb-4">
                <div class="row pt-1">
                  <div class="col-6 mb-3">
                    <h6>Nombre</h6>
                    <p class="text-muted"><%=usuario.nombre%></p>
                  </div>
                  <div class="col-6 mb-3">
                    <h6>Apellidos</h6>
                    <p class="text-muted"><%=usuario.apellido%></p>
                  </div>
                </div>
                <div class="row pt-1">
                  <div class="col-6 mb-3">
                    <h6>Email</h6>
                    <p class="text-muted"><%=usuario.email%></p>
                  </div>
                  <div class="col-6 mb-3">
                    <h6>Contraseña</h6>
                    <p class="text-muted"><%=usuario.contrasena%></p>
                  </div>
                </div>
                <div class="d-flex justify-content-center">
                  <a href="https://www.facebook.com/"><i class="fab fa-facebook-f fa-lg me-3 iconos"></i></a>
                  <a href="https://twitter.com/"><i class="fab fa-twitter fa-lg me-3 iconos"></i></a>
                  <a href="https://www.instagram.com/"><i class="fab fa-instagram fa-lg iconos"></i></a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>